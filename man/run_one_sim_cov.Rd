% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{run_one_sim_cov}
\alias{run_one_sim_cov}
\title{Fit one simulated trial under an arbitrary within-subject
covariance structure and extract the treatment-by-time estimate}
\usage{
run_one_sim_cov(
  times,
  n_per_group,
  beta_0,
  beta_1,
  beta_2,
  beta_3,
  v_mat,
  structure,
  dat = NULL
)
}
\arguments{
\item{times}{Numeric vector of visit times (months).}

\item{n_per_group}{Integer, number of subjects per arm.}

\item{beta_0}{Numeric, fixed intercept.}

\item{beta_1}{Numeric, fixed time slope (control arm).}

\item{beta_2}{Numeric, baseline group effect.}

\item{beta_3}{Numeric, treatment-by-time interaction.}

\item{v_mat}{A k x k within-subject covariance matrix, where
\code{k = length(times)}, as produced by \code{\link[=cov_random_slope]{cov_random_slope()}},
\code{\link[=cov_compound_symmetry]{cov_compound_symmetry()}}, or \code{\link[=cov_ar1_error]{cov_ar1_error()}}.}

\item{structure}{Character, one of \code{"compound_symmetry"} or
\code{"ar1_error"}, selecting the fitted correlation structure.}

\item{dat}{Optional pre-generated data frame from
\code{\link[=simulate_trial_cov]{simulate_trial_cov()}}; if \code{NULL} (the default) a new dataset is
generated internally from \code{v_mat}.}
}
\value{
A one-row data frame with columns \code{est}, \code{se}, \code{pval},
\code{ci_lo}, \code{ci_hi}. All are \code{NA} if the model fit failed to
converge.
}
\description{
Generalizes \code{\link[=run_one_sim]{run_one_sim()}} to fit via \code{nlme::gls()} with a
correlation structure matched to the covariance family used to
generate the data (compound symmetry or AR(1) plus measurement
error), rather than a random-intercept, random-slope \code{nlme::lme()}
model. Used for the compound-symmetry and AR(1)-plus-error cells
of the full-factorial Monte Carlo grid (2026-08-16 review, issue
2.4).
}
