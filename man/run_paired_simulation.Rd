% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{run_paired_simulation}
\alias{run_paired_simulation}
\title{Run a paired Monte Carlo comparison of two visit designs}
\usage{
run_paired_simulation(
  times_a,
  times_b,
  n_per_group,
  beta_0,
  beta_1,
  beta_2,
  beta_3,
  tau_0,
  tau_1,
  tau_01,
  sigma,
  n_sims,
  seed = NULL
)
}
\arguments{
\item{times_a, times_b}{Numeric vectors of visit times for the two
designs, of equal length \code{k}.}

\item{n_per_group}{Integer, number of subjects per arm.}

\item{beta_0}{Numeric, fixed intercept.}

\item{beta_1}{Numeric, fixed time slope (control arm).}

\item{beta_2}{Numeric, baseline group effect.}

\item{beta_3}{Numeric, treatment-by-time interaction.}

\item{tau_0}{Numeric, SD of the random intercept.}

\item{tau_1}{Numeric, SD of the random slope.}

\item{tau_01}{Numeric, covariance between random intercept and slope.}

\item{sigma}{Numeric, residual SD.}

\item{n_sims}{Integer, number of Monte Carlo replications.}

\item{seed}{Integer, RNG seed (set once before the loop).}
}
\value{
A list with elements \code{results_a}, \code{results_b} (data
frames as returned by \code{\link[=run_one_sim]{run_one_sim()}}, with a \code{rep} column
added) and \code{paired_diff}, the per-replication difference in
\code{est} between design A and design B computed only for
replications where both designs converged.
}
\description{
Uses common random numbers: for each replication, the same
subject-level random effects and residual errors are reused for
both designs (matched on subject and visit index), so that the
Monte Carlo comparison of the two designs is a paired comparison
with much smaller Monte Carlo error than two independent
simulation arms (review issue 2.2).
}
